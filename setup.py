from setuptools import setup

# This is not a Python package, but we put a setup.py file anyway so that it can be pip installable. Kinda dirty but it works.
setup(name='jquery-tokenitup',
      version='0.1',)
